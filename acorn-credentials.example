# Acorn developer credentials
# Copy to ~/.acorn/credentials and set chmod 600
# Source this file in your shell profile: source ~/.acorn/credentials
#
# This file is never committed to git. Generate a strong password once
# and store it in your password manager as "ACORN_DEV_PASSWORD".

export ACORN_DEV_PASSWORD="CHANGE_ME_STRONG_PASSWORD_HERE"
